<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Coming Soon - SFR Aromatics</title>
  <style>
    /* General Reset */
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: 'Arial', sans-serif;
      background-color: #000; /* Black Background */
      color: #ffd700; /* Golden Text */
    }

    /* Main Container */
    .container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100%;
      text-align: center;
    }

    /* Logo */
    .logo {
      width: 200px; /* Adjust width */
      height: auto;
      margin-bottom: 2rem;
    }

    /* Title and Subtitle */
    .title {
      font-size: 3rem;
      font-weight: bold;
      color: #ffd700; /* Golden Text */
      margin-bottom: 1rem;
    }

    .subtitle {
      font-size: 1.5rem;
      margin-bottom: 2rem;
      color: #ffd700; /* Golden Text */
    }

    /* Notify Me Form */
    .notify-form {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    input[type="email"] {
      padding: 0.7rem;
      width: 300px;
      border: 2px solid #ffd700; /* Golden Border */
      border-radius: 5px;
      outline: none;
      font-size: 1rem;
      background-color: #000; /* Black Background */
      color: #ffd700; /* Golden Text */
    }

    button {
      padding: 0.7rem 2rem;
      background-color: #00008b; /* Dark Blue Button */
      color: #ffd700; /* Golden Text */
      border: 2px solid #ffd700; /* Golden Border */
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    button:hover {
      background-color: #ffd700; /* Golden Background */
      color: #000; /* Black Text */
    }

    /* Footer */
    .footer {
      margin-top: 2rem;
      font-size: 0.8rem;
      color: #ffd700; /* Golden Text */
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Logo -->
    <img src="/images/AROMATICS.PNG" alt="SFR Aromatics Logo" class="logo">
    
    <!-- Title -->
    <h1 class="title">SFR Aromatics</h1>
    <p class="subtitle">We're launching something amazing soon. Stay tuned!</p>
    
    <!-- Notify Me Form -->
    <form class="notify-form">
      <input type="email" placeholder="Enter your email" required>
      <button type="submit">Notify Me</button>
    </form>
    
    <!-- Footer -->
    <footer class="footer">
      <p>&copy; 2025 SFR Aromatics. All rights reserved.</p>
    </footer>
  </div>
</body>
</html>

